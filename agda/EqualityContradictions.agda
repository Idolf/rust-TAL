open import Types
open import Data.Empty
open import Relation.Binary.Core

ρ≡α→⊥ : ∀ {σ} → ρ ≡ α σ → ⊥
ρ≡α→⊥ ()

ρ≡β→⊥ : ∀ {σ ♯b} → ρ ≡ β σ ♯b → ⊥
ρ≡β→⊥ ()

ρ≡≤a→⊥ : ∀ {ℓ₁ ℓ₂ σ} → ρ ≡ ℓ₁ ≤a ℓ₂ / σ → ⊥
ρ≡≤a→⊥ ()

α≡β→⊥ : ∀ {σ σ' ♯b} → α σ ≡ β σ' ♯b → ⊥
α≡β→⊥ ()

α≡≤a→⊥ : ∀ {σ σ' ℓ₁ ℓ₂} → α σ ≡ ℓ₁ ≤a ℓ₂ / σ' → ⊥
α≡≤a→⊥ ()

β≡≤a→⊥ : ∀ {σ σ' ♯b ℓ₁ ℓ₂} → β σ ♯b ≡ ℓ₁ ≤a ℓ₂ / σ' → ⊥
β≡≤a→⊥ ()

type≡γ→⊥ : ∀ {τ} → type τ ≡ γ → ⊥
type≡γ→⊥ ()
